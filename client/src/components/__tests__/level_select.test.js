import '@testing-library/react'
import { test } from '@jest/globals'
import CreateGame from '../CreateGame'

test('Testing the create game forms mission select function', () => {
  expect(true).toBe(true)
})